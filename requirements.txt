# V.E.R.A. — Virtual Entity for Real-time Assistance
# Requirements — install with: pip install -r requirements.txt

# ── Gemini Live (LLM + Voice + Vision) ────────────────────────────────────────
google-genai>=1.0.0         # Google Gemini SDK (Multimodal Live API)
pyaudiowpatch>=0.2.12       # Real-time mic input & speaker output (pre-built Windows wheels, drop-in pyaudio replacement)

# ── Screen Vision ──────────────────────────────────────────────────────────────
mss>=9.0.0                  # Ultra-fast cross-platform screen capture
Pillow>=10.0.0              # Image processing / JPEG encoding for screen frames

# ── CLI & Terminal UI ──────────────────────────────────────────────────────────
rich>=13.0.0                # Rich terminal formatting
typer>=0.12.0               # CLI argument parsing
readchar>=4.0.0             # Arrow-key navigation

# ── Configuration ──────────────────────────────────────────────────────────────
pyyaml>=6.0                 # config.yaml parsing

# ── Web & Research ─────────────────────────────────────────────────────────────
requests>=2.31.0            # HTTP requests
beautifulsoup4>=4.12.0      # HTML parsing / web scraping
playwright>=1.40.0          # Browser automation (headless Chrome)

# ── Desktop & App Automation ───────────────────────────────────────────────────
pyautogui>=0.9.54           # Mouse / keyboard automation
pynput>=1.7.6               # Low-level input listener
pywinauto>=0.6.9            # Windows window control
pygetwindow>=0.0.9          # Cross-platform window listing

# ── System Utilities ───────────────────────────────────────────────────────────
psutil>=5.9.0               # Process / system monitoring
numpy>=1.24.0               # Numeric processing